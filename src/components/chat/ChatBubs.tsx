import { Message } from "~/types/message";

interface ChatBubsProps {
    message: Message;
}

export const ChatBubs = ({ message }: ChatBubsProps) => {
    //check session
    //check is CurrentUser

    return (
        <div className="flex">
        </div>
    )
}